/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['npr.brightspotcdn.com', 'image.simplecastcdn.com']
  }
}

module.exports = nextConfig
